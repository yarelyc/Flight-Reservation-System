<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="#ffffff"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.csumb.chin3816.flightreservationsystemotterairways.MainActivity">

    <Button

        android:layout_height="wrap_content"
        android:layout_width="180dp"
        android:text="Create Account"
        android:background="#FFFFFF"
        android:id="@+id/createAccountButton"
        android:layout_marginTop="100dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_height="wrap_content"
        android:layout_width="180dp"
        android:background="#FFFFFF"
        android:text="Reservation Seat"
        android:id="@+id/reservationSeatButton"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/createAccountButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:text="Manage System"
        android:background="#FFFFFF"
        android:id="@+id/manageSystemButton"
        android:layout_marginTop="20dp"
        android:layout_centerVertical="true"
        android:layout_below="@+id/reservationSeatButton"
        android:layout_alignStart="@+id/reservationSeatButton" />

    <Button
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:text="Cancel Reservation"
        android:background="#FFFFFF"
        android:id="@+id/cancelReservationButton"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/manageSystemButton"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
